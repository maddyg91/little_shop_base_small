<h3>Review for item: <%= @review.item_id %></h3>
  <p>Title: <%= @review.title %></p>
  <p>Description: <%= @review.description %></p>
  <p>Rating: <%= @review.rating %></p>
<%= link_to "Disable", item_review_path(@item, @review), method: :patch %>
